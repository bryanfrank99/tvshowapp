# Tasks: Capacitor shell + antibloqueo

- [ ] 1. `npm i @capacitor/core @capacitor/cli` + `cap init` + `cap add android` → verificar: `android/` generado
- [ ] 2. Leer `BridgeWebViewClient.java` y `BridgeWebChromeClient.java` instalados → verificar: APIs confirmadas
- [ ] 3. `AdBlockWebViewClient.java` + `adhosts.txt` + wiring en `MainActivity.java` → verificar: compila (o revisión)
- [ ] 4. `fastlane/` + `.gitignore` + README → verificar: `git status` limpio de artefactos
- [ ] 5. Intentar `assembleDebug` si hay SDK; si no, guía de build
